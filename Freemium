local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ForceCheckbox = false 
Library.ShowToggleFrameInKeybinds = true 

local Window = Library:CreateWindow({
	Title = "ShinyLeak | Nights of Eternity",
	Footer = "version: 1.0",
	Icon = 95816097006870,
	NotifySide = "Right",
	ShowCustomCursor = true,
})

-- 1. Create your Tabs correctly (Name, Icon)
local Tabs = {
	Main = Window:AddTab("AutoFarm", "swords"),
	Buy = Window:AddTab("Auto Buy", "shopping-cart"),
	UISettings = Window:AddTab("UI Settings", "settings")
}

-- ==========================================
--               AUTOFARM TAB
-- ==========================================
local FarmGroup = Tabs.Main:AddGroupbox({
	Side = "Left",
	Name = "Farming",
	Description = "Main farming features",
})

FarmGroup:AddToggle("AutoFarmToggle", {
	Text = "Enable AutoFarm",
	Tooltip = "Automatically farms the game",
	Default = false, 
	
	Callback = function(Value)
		print("[AutoFarm] toggled:", Value)
        -- TODO: Add your AutoFarm while loop or RunService logic here
	end,
})

-- ==========================================
--               AUTO BUY TAB
-- ==========================================
-- Left Side: Survivors
local BuySurvivorsGroup = Tabs.Buy:AddGroupbox({
	Side = "Left", 
	Name = "Survivors",
	Description = "Auto purchase Survivors",
})

BuySurvivorsGroup:AddToggle("AutoBuySurvivors", {
	Text = "Auto Buy Survivors",
	Default = false,
	Callback = function(Value)
		print("[Survivors] toggled:", Value)
        -- TODO: Add RemoteEvent firing logic to buy survivors
	end,
})

-- Right Side: Killers
local BuyKillersGroup = Tabs.Buy:AddGroupbox({
	Side = "Right", 
	Name = "Killers",
	Description = "Auto purchase Killers",
})

BuyKillersGroup:AddToggle("AutoBuyKillers", {
	Text = "Auto Buy Killers",
	Default = false,
	Callback = function(Value)
		print("[Killers] toggled:", Value)
        -- TODO: Add RemoteEvent firing logic to buy killers
	end,
})

-- ==========================================
--               UI SETTINGS TAB
-- ==========================================

Library:SetWatermark("ShinyLeak - Nights of Eternity")
Library:Notify("Script loaded successfully!", 3)

-- Set up the Theme Manager
ThemeManager:SetLibrary(Library)
ThemeManager:SetFolder("ShinyLeak")
ThemeManager:BuildGroupBox(Tabs.UISettings:AddGroupbox({
    Side = "Left",
    Name = "Theme",
}))

-- Set up the Save Manager (Configs)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({'UISettings'})
SaveManager:SetFolder("ShinyLeak/NightsOfEternity")
SaveManager:BuildConfigSection(Tabs.UISettings)
SaveManager:LoadAutoloadConfig()
